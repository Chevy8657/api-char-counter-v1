# api-char-counter-v1
Stateless utility API that counts the total number of characters in a string, including spaces and punctuation. Designed for deterministic, high-throughput text analysis with no state, no storage, and a single fixed endpoint.
